[package]
name = "hatch-rs-test-pyodide"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "pyodide_project"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["abi3", "extension-module"] }

[profile.release]
panic = "abort"
